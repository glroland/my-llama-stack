apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ollama-llama-stack.fullname" . }}-test-connection"
  labels:
    {{- include "ollama-llama-stack.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ollama-llama-stack.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
