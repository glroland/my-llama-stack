apiVersion: v1
kind: Service
metadata:
  name: {{ include "ollama-llama-stack.fullname" . }}
  labels:
    {{- include "ollama-llama-stack.labels" . | nindent 4 }}
spec:
  ipFamilies:
    - IPv4
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.image.port }}
  internalTrafficPolicy: Cluster
  type: {{ .Values.service.type }}
  sessionAffinity: None
  selector:
    {{- include "ollama-llama-stack.selectorLabels" . | nindent 4 }}
