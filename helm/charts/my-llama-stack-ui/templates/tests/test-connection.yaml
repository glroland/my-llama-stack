apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "my-llama-stack-ui.fullname" . }}-test-connection"
  labels:
    {{- include "my-llama-stack-ui.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "my-llama-stack-ui.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
